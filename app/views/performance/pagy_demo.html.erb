<h1>Pagy 比較テスト</h1>

<h2>テスト内容: Pagy標準版 vs Pagy Countless比較</h2>

<% if @duration.is_a?(Hash) %>
  <p>Pagy 標準 (データのみ): <%= sprintf("%.4f", @duration[:data_only][0] * 1000) %> ms</p>
  <p>Pagy Countless (データのみ): <%= sprintf("%.4f", @duration[:data_only][1] * 1000) %> ms</p>
  <p>Pagy 標準 (ビュー含む): <%= sprintf("%.4f", @duration[:with_view][0] * 1000) %> ms</p>
  <p>Pagy Countless (ビュー含む): <%= sprintf("%.4f", @duration[:with_view][1] * 1000) %> ms</p>
<% end %>

<% pagy_obj = local_assigns[:pagy] || @pagy %>
<% articles = local_assigns[:articles] || @articles %>

<% if pagy_obj && articles %>
  <p>ID一覧: <%= articles.map(&:id).join(', ') %></p>
  <%== pagy_nav(pagy_obj) %>
<% end %>


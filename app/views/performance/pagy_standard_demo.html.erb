<h1>Pagy 標準版テスト</h1>

<h2>テスト内容: COUNTクエリ付きのPagy標準実装</h2>

<% if @duration %>
  <p>データのみ: <%= sprintf('%.4f', @duration[:data_only][0] * 1000) %> ms</p>
  <p>ビュー含む: <%= sprintf('%.4f', @duration[:with_view][0] * 1000) %> ms</p>
<% end %>

<% if @pagy && @articles %>
  <p>ID一覧: <%= @articles.map(&:id).join(', ') %></p>
  <%== pagy_nav(@pagy) %>
<% end %>

